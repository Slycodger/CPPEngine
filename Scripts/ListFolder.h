#pragma once

#include "ObjectClasses.h"
#ifdef ListFolder_MAIN

#include <Windows.h>
#include "Button.h"

#endif

void visualFolder(std::string Folder, Object*& Target);

namespace ListFolder
{
	void start();
}